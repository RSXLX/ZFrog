## 🐸 随机探索 MVP 版本 - 最小化迭代方案

### 一、MVP 核心原则

**目标：用最少的改动，快速验证「随机探索」功能**

**策略：**
- ✅ 复用现有 Travel 流程，最小化代码修改
- ✅ 先跑通一种发现方式（最简单的）
- ✅ 不改数据库结构，用现有字段
- ✅ AI Prompt 微调即可，不需要重写

---

### 二、MVP 功能范围

| 功能 | MVP 包含 | 未来版本 |
|-----|---------|---------|
| 随机发现地址 | ✅ 从最新区块采样 | 多种发现策略 |
| 地址过滤 | ✅ 基础过滤（排除合约） | 智能过滤 |
| 钱包分析 | ✅ 复用现有 ChainService | - |
| 数据存储 | ✅ 复用 Travel 表 | 新增 WalletSnapshot 表 |
| AI 日记 | ✅ 修改 Prompt | 多种风格 |
| 前端入口 | ✅ 一个「随机探险」按钮 | 配置项 |

---

### 三、基于现有数据结构的实现方案

#### 现有 Travel 表结构分析

```prisma
model Travel {
  id            String
  userId        String
  
  targetChain   ChainType      // ✅ 保留：用户选择探索哪条链
  targetWallet  String         // ✅ 保留：初始传入特殊值标识随机模式
  targetENS     String?
  
  duration      Int
  stage         TravelStage
  
  startedAt     DateTime?
  completedAt   DateTime?
  
  postcard      Postcard?
}
```

#### MVP 方案：不改数据库，用约定标识随机模式

**关键技巧：用特殊的 `targetWallet` 值表示随机模式**

```typescript
// 约定：targetWallet = "RANDOM" 表示随机探索
const RANDOM_TRAVEL_MARKER = "RANDOM";

// 创建随机探索的 Travel 记录
const travel = await prisma.travel.create({
  data: {
    userId,
    targetChain: "ethereum",
    targetWallet: RANDOM_TRAVEL_MARKER,  // 🔑 关键标识
    duration: 3600,
    stage: "PREPARING",
  }
});
```

**优势：**
- ❌ 不需要改数据库 Schema
- ❌ 不需要迁移数据
- ✅ 向后兼容（老的 Travel 记录不受影响）

---

### 四、核心代码实现（MVP 版）

#### 1. 新增：地址发现服务（最简单版）

```typescript
// services/travel/address.discovery.ts

import { ethers } from 'ethers';

export class AddressDiscovery {
  constructor(private provider: ethers.Provider) {}

  /**
   * MVP 方法：从最新区块中随机获取一个钱包地址
   */
  async discoverFromLatestBlock(chain: ChainType): Promise<string> {
    // 1. 获取最新区块
    const latestBlock = await this.provider.getBlock('latest', true);
    if (!latestBlock || !latestBlock.transactions) {
      throw new Error('Failed to fetch latest block');
    }

    // 2. 收集所有地址（from 和 to）
    const addresses = new Set<string>();
    
    for (const tx of latestBlock.transactions) {
      if (typeof tx === 'string') continue; // 跳过 hash-only 模式
      
      if (tx.from) addresses.add(tx.from.toLowerCase());
      if (tx.to) addresses.add(tx.to.toLowerCase());
    }

    // 3. 转换为数组并过滤
    const addressList = Array.from(addresses);
    const validAddresses = await this.filterValidAddresses(addressList);

    if (validAddresses.length === 0) {
      // 递归重试：获取上一个区块
      return this.discoverFromPreviousBlock(chain, latestBlock.number - 1);
    }

    // 4. 随机选择一个
    const randomIndex = Math.floor(Math.random() * validAddresses.length);
    return validAddresses[randomIndex];
  }

  /**
   * 过滤出有效的钱包地址
   */
  private async filterValidAddresses(addresses: string[]): Promise<string[]> {
    const KNOWN_EXCHANGES = [
      '0x...binance...', 
      '0x...coinbase...',
      // 添加主流交易所地址（可以预先准备一个黑名单）
    ];

    const valid: string[] = [];

    for (const addr of addresses) {
      // 排除交易所地址
      if (KNOWN_EXCHANGES.includes(addr)) continue;

      // 排除合约地址（检查是否有 code）
      const code = await this.provider.getCode(addr);
      if (code !== '0x') continue; // 有 code = 合约

      // 检查是否有一定的交易历史（避免全新地址）
      const txCount = await this.provider.getTransactionCount(addr);
      if (txCount < 10) continue; // 至少 10 笔交易

      // 检查是否有余额（避免空钱包）
      const balance = await this.provider.getBalance(addr);
      if (balance < ethers.parseEther('0.01')) continue; // 至少 0.01 ETH

      valid.push(addr);
    }

    return valid;
  }

  /**
   * 兜底方法：从前一个区块查找
   */
  private async discoverFromPreviousBlock(
    chain: ChainType, 
    blockNumber: number
  ): Promise<string> {
    const block = await this.provider.getBlock(blockNumber, true);
    // ... 复用上面的逻辑
  }
}
```

---

#### 2. 修改：TravelService（添加随机模式支持）

```typescript
// services/travel/travel.service.ts

export class TravelService {
  constructor(
    private prisma: PrismaClient,
    private chainService: ChainService,
    private addressDiscovery: AddressDiscovery, // 新增
  ) {}

  /**
   * 新增：开始随机探索
   */
  async startRandomTravel(userId: string, dto: StartRandomTravelDto) {
    // 1. 创建 Travel 记录（targetWallet = "RANDOM"）
    const travel = await this.prisma.travel.create({
      data: {
        userId,
        targetChain: dto.targetChain,
        targetWallet: 'RANDOM', // 🔑 标识随机模式
        duration: dto.duration,
        stage: 'PREPARING',
      },
    });

    // 2. 加入任务队列（和普通旅行一样）
    await this.travelQueue.add('execute-travel', {
      travelId: travel.id,
      isRandom: true, // 🔑 传递标志
    });

    return travel;
  }

  /**
   * 原有方法：开始定向旅行（保持不变）
   */
  async startDirectedTravel(userId: string, dto: StartDirectedTravelDto) {
    // ... 原有逻辑不变
  }
}
```

---

#### 3. 修改：TravelWorker（核心执行器）

```typescript
// workers/travel.worker.ts

export class TravelWorker {
  async executeTravelJob(job: Job<TravelJobData>) {
    const { travelId, isRandom } = job.data;
    
    const travel = await this.prisma.travel.findUnique({
      where: { id: travelId },
      include: { user: { include: { frog: true } } },
    });

    if (!travel) throw new Error('Travel not found');

    try {
      // === 阶段 1: 准备出发 ===
      await this.updateStage(travelId, 'TRAVELING');
      await this.notifyUser(travel.userId, {
        type: 'travel_start',
        message: '呱呱准备出发探险啦！🎒',
      });

      // === 阶段 2: 发现目标（随机模式特有） ===
      let targetWallet = travel.targetWallet;
      
      if (isRandom || targetWallet === 'RANDOM') {
        // 🔑 随机发现地址
        targetWallet = await this.addressDiscovery.discoverFromLatestBlock(
          travel.targetChain
        );

        // 更新 Travel 记录
        await this.prisma.travel.update({
          where: { id: travelId },
          data: { targetWallet },
        });

        // 通知用户
        await this.notifyUser(travel.userId, {
          type: 'travel_discovery',
          message: `呱发现了一个有趣的钱包！${targetWallet.slice(0, 6)}...`,
        });

        // 模拟发现过程（增加仪式感）
        await this.delay(5000);
      }

      // === 阶段 3: 观察分析（和原有流程一致）===
      await this.updateStage(travelId, 'OBSERVING');
      await this.notifyUser(travel.userId, {
        type: 'travel_observing',
        message: '呱正在仔细观察这个钱包...',
      });

      const analysis = await this.chainService.analyzeAddress(
        targetWallet,
        travel.targetChain
      );

      // === 阶段 4: 生成日记 ===
      await this.updateStage(travelId, 'WRITING_DIARY');
      
      const diary = await this.aiService.generateDiary({
        frogName: travel.user.frog?.name || '小青蛙',
        personality: travel.user.frog?.personality || 'COMEDIAN',
        targetAddress: targetWallet,
        targetChain: travel.targetChain,
        addressAnalysis: analysis,
        isRandomTravel: isRandom, // 🔑 传递给 AI
      });

      // === 阶段 5: 生成明信片（和原有流程一致）===
      const postcard = await this.postcardService.create({
        travelId,
        diary: diary.text,
        mood: diary.mood,
        // ...
      });

      // === 阶段 6: 完成 ===
      await this.updateStage(travelId, 'COMPLETED');
      await this.notifyUser(travel.userId, {
        type: 'travel_complete',
        postcardId: postcard.id,
      });

    } catch (error) {
      await this.handleTravelError(travelId, error);
    }
  }

  private delay(ms: number): Promise<void> {
    return new Promise(resolve => setTimeout(resolve, ms));
  }
}
```

---

#### 4. 修改：AI Prompt（最小改动）

```typescript
// services/ai/prompts/diary.prompt.ts

export class DiaryPromptBuilder {
  static systemPrompt = `你是一只可爱的旅行青蛙，名叫「{frog_name}」，性格是「{personality}」。

你刚刚完成了一次{travel_type}，现在要写一篇旅行日记给主人看。

{travel_type} 说明：
- 定向旅行：主人让你去观察一个特定的钱包
- 随机探险：你自己随便逛，意外发现了一个陌生钱包 🔍

性格说明：
- 哲学家: 深沉、爱思考
- 段子手: 吐槽、搞笑
- 诗人: 浪漫、文艺
- 八卦蛙: 爱爆料

输出要求：
1. 开头用「呱！{opening}」
   - 定向旅行：「呱！我去主人说的地方看了看...」
   - 随机探险：「呱！今天随便逛逛，意外发现了...」 
2. 字数 150-250 字
3. 要有趣、有细节、有洞察
4. 结尾留下祝福或调侃

输出 JSON 格式：
{
  "text": "日记正文",
  "mood": "excited|curious|shocked|philosophical|amused",
  "highlight": "最有趣的一点"
}`;

  static build(config: DiaryConfig): string {
    const travelType = config.isRandomTravel ? '随机探险' : '定向旅行';
    const opening = config.isRandomTravel 
      ? '今天随便逛逛，意外发现了一个陌生钱包' 
      : '我去主人说的地方看了看';

    return `
## 青蛙信息
- 名字：${config.frogName}
- 性格：${config.personality}
- 旅行类型：${travelType} ${config.isRandomTravel ? '🔍' : '🎯'}

## 访问的地址信息
- 地址：${config.targetAddress}
- ENS：${config.targetENS || '无'}
- 所在链：${config.targetChain}
- 账户年龄：${config.addressAnalysis.accountAge}
- 主要持仓：${this.formatHoldings(config.addressAnalysis.holdings)}
- 交互协议：${config.addressAnalysis.interactedProtocols.join(', ')}
- 特殊标签：${config.addressAnalysis.tags.join(', ')}
- 特殊发现：${config.addressAnalysis.specialFindings.join('; ')}

请根据以上信息，用青蛙的性格写一篇旅行日记。
${config.isRandomTravel ? '记得强调这是你意外发现的，表现出探险的新奇感！' : ''}
    `.trim();
  }
}
```

---

#### 5. 新增：API 路由

```typescript
// routes/travel.routes.ts

import { FastifyInstance } from 'fastify';

export async function travelRoutes(fastify: FastifyInstance) {
  
  // 原有 API（保持不变）
  fastify.post('/travel/start', async (request, reply) => {
    const { targetWallet, targetChain, duration } = request.body;
    
    const travel = await travelService.startDirectedTravel(request.user.id, {
      targetWallet,
      targetChain,
      duration,
    });
    
    return { success: true, data: { travelId: travel.id } };
  });

  // 新增 API（随机探索）
  fastify.post('/travel/start-random', async (request, reply) => {
    const { targetChain, duration } = request.body;
    
    // 🔑 调用新方法
    const travel = await travelService.startRandomTravel(request.user.id, {
      targetChain: targetChain || 'ethereum', // 默认以太坊
      duration: duration || 3600,
    });
    
    return { 
      success: true, 
      data: { 
        travelId: travel.id,
        mode: 'random',
        message: '青蛙出发去随机探险啦！',
      } 
    };
  });
}
```

---

#### 6. 前端改动（最小化）

```typescript
// 原有按钮（保持不变）
<button onClick={handleStartTravel}>
  指定地址旅行 🎯
</button>

// 新增按钮
<button onClick={handleStartRandomTravel}>
  随机探险 🎲
</button>

// 处理函数
const handleStartRandomTravel = async () => {
  const response = await fetch('/api/travel/start-random', {
    method: 'POST',
    body: JSON.stringify({
      targetChain: 'ethereum',
      duration: 3600,
    }),
  });
  
  const { data } = await response.json();
  
  // 跳转到旅行详情页（和原有流程一样）
  navigate(`/travel/${data.travelId}`);
};
```

---

### 五、MVP 开发任务清单

| 任务 | 工作量 | 优先级 |
|-----|-------|-------|
| 1. 创建 `AddressDiscovery` 服务 | 2-3 小时 | P0 |
| 2. 修改 `TravelService` 添加 `startRandomTravel` | 1 小时 | P0 |
| 3. 修改 `TravelWorker` 添加发现逻辑 | 2-3 小时 | P0 |
| 4. 调整 AI Prompt 支持随机模式 | 1 小时 | P0 |
| 5. 新增 API 路由 `/travel/start-random` | 0.5 小时 | P0 |
| 6. 前端新增「随机探险」按钮 | 1 小时 | P0 |
| 7. 测试 & 调试 | 2-3 小时 | P0 |
| **总计** | **约 1-1.5 天** | - |

---

### 六、MVP 验证指标

部署后重点观察：

| 指标 | 目标 | 说明 |
|-----|------|------|
| **地址发现成功率** | > 90% | 能成功从区块中找到有效地址 |
| **地址有趣度** | > 60% | 用户觉得发现的地址"有故事" |
| **日记质量** | > 70% 好评 | AI 生成的日记读起来自然 |
| **功能使用率** | > 30% | 用户愿意尝试随机探索 |
| **错误率** | < 5% | 系统稳定性 |

---

### 七、MVP 之后的迭代计划

**Phase 2（如果 MVP 验证成功）:**
1. 新增数据库字段 `Travel.mode` 和 `Travel.discoveryMethod`
2. 支持多种发现策略（DEX、NFT、名人地址）
3. 新增 `WalletSnapshot` 表存储完整分析数据
4. 优化地址过滤算法（更精准的"有趣度"判断）

**Phase 3:**
1. 用户可配置探索偏好（"我想看 DeFi 玩家" / "我想看 NFT 收藏家"）
2. 探索历史记录可回溯
3. 支持跨链随机探索（多条链混合）

---

### 八、风险与应对

| 风险 | 应对方案 |
|-----|---------|
| **找不到有效地址** | 实现重试机制，最多回溯 10 个区块 |
| **分析超时** | 设置 30 秒超时，失败则返回简化版分析 |
| **AI 生成质量不稳定** | 准备 fallback 模板，关键时刻用模板 |
| **RPC 速率限制** | 使用 Alchemy/Infura 付费层，或加缓存 |

---

### 总结

**MVP 核心思路：**
1. ✅ 用 `targetWallet = "RANDOM"` 标识随机模式（不改数据库）
2. ✅ 新增最简单的地址发现方法（从最新区块采样）
3. ✅ 在 Worker 中插入发现逻辑（最小化改动）
4. ✅ AI Prompt 微调即可（加一个标志位）
5. ✅ 前端加一个按钮（1 行代码）
